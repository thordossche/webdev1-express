extends ../layout

block content
    h1= title

    ul
        each auction in auction_list
            li
                a(href=auction.url) #{auction.name}
                |  (#{auction.running_time})

        else
            li There are no auctions.